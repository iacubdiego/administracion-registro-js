<!DOCTYPE html>
<html>
  <%- include('./partials/head') %>
  <body>    
    <%- include("./partials/header") %>    
    <!-- Page content -->

    
    <%- include("./partials/card") %>    

    <%- include("./partials/card2") %>    


    <div class="ps-content">
      <!-- <div>
        <select name="estado" id="filtro_estado">
          Estado
        </select>
        <input type="text" name="bus_general" id="bus_general" value="" class="form-control txtfiltro" placeholder="Busqueda por Club / Cuit" autocomplete="off">

      </div> -->
      <!-- About Section -->
      <hr />
      
      <!-- Table Section -->
      <%- include("./partials/table") %>    
    </div>
                <br />
                <div class="ps-row ps-padding-64" id="menu">
                  <div class="ps-col l6 ps-padding-large">
                    <h1 class="ps-center">Nuentro Menu</h1>
                <div>
                  
                  <% for(let index=0; index < menu.length; index++) { %>
                    <a href="/detalle/<%= menu[index].id %>">
                      <h4><%= menu[index].titulo %></h4>
                      <p class="ps-text-grey">
                  <%= menu[index].description %>
                  <%= menu[index].currency %>
                  <%= menu[index].price %>
                </p>
              </a>
              <!-- eliminar producto -->
              <form action="/products/<%= menu[index].id %>?_method=DELETE" method="post">
              <button type="submit" class="ps-btn ps-btn-danger">Eliminar</button>
            </form>
            <br />
            <% } %>
        
        </div>
      </div>
    </div>

        <!-- <div class="ps-col l6 ps-padding-large ps-img-menu">
          <img
            src="/images/menu.jpg"
            class="ps-round ps-image ps-opacity-min"
            alt="Menu"
          />
        </div> -->
           
      <!-- Footer -->
      <%- include("./partials/footer") %>
  </body>
</html>
